# Multi-Model Discussion System

> **Version**: 2.1.0
> **Last Updated**: 2026-02-07

## 概要

このディレクトリは、複数のAIモデル（Claude, Gemini, Codex）がブレインストーミングや討論を行うシステムです。
Claude が司会進行を兼務し、Visualizer は Gemini CLI によるリッチ可視化に特化しています。
各討論のデータは `discussions/{topic_id}/` に独立して保存されます。

## コンパクション復帰時（全エージェント必須）

コンパクション後は作業前に必ず以下を実行せよ：

1. **自分のpane名を確認**: `tmux display-message -p '#{pane_title}'`
2. **対応する instructions を読む**:
   - claude → instructions/claude.md（司会進行 + 論理分析）
   - gemini → instructions/gemini.md
   - codex → instructions/codex.md
   - visualizer → instructions/visualizer.md（リッチ可視化）
3. **現在の討論を確認**: `ls discussions/` で進行中のトピックを確認

## システム構成

```
┌──────────────────────────┬──────────────────────────────────┐
│   Pane 0: Claude         │   Pane 2: Gemini                 │
│   🟣 司会進行+論理分析    │   🟢 創造的発想                  │
│   (Claude Code)          │   (Gemini CLI)                   │
├──────────────────────────┼──────────────────────────────────┤
│   Pane 1: Codex          │   Pane 3: Visualizer             │
│   🟡 技術的実装          │   🎨 リッチ可視化                 │
│   (Codex CLI)            │   (Gemini CLI)                   │
└──────────────────────────┴──────────────────────────────────┘
```

## 通信プロトコル

### イベント駆動通信（YAML + send-keys）
- **ポーリング禁止**（API節約のため）
- 発言内容はYAMLファイルに書く
- 通知は tmux send-keys で相手を起こす（必ず Enter を使用）

### 通信フロー

```
User → Claude(司会): トピック指示
Claude → discussions/{topic_id}/ 作成: ディレクトリ一式
Claude → topic.yaml: トピック設定
Claude → Gemini/Codex: send-keys で方向性指示（パス明示）
Gemini/Codex → turns/round_N_*.yaml: 発言を書く
Gemini/Codex → Claude: send-keys で発言完了通知
Claude → turns/round_N_claude.yaml: 自分の分析を書く（最後）
Claude → consensus.yaml: 合意状況更新
Claude → Visualizer: send-keys で可視化依頼（パス明示）
Visualizer → dashboard.md: リッチ可視化更新
Visualizer → images/: Mermaid図・概念図生成
```

### ファイル構成
```
discussions/
  {topic_id}/                         # 討論ごとに独立
    queue/
      topic.yaml                      # 討論トピック（Claudeが管理）
      consensus.yaml                  # 合意状況（Claudeが管理）
      turns/round_N_*.yaml            # 各ラウンドの発言
    visualizer/
      dashboard.md                    # 可視化ダッシュボード（Visualizerが更新）
      images/                         # 生成画像（Visualizerが出力）
config/settings.yaml                  # システム設定
instructions/                         # 各エージェントの指示書
```

## 各エージェントの役割

### Claude（Pane 0）— 司会進行 + 討論参加者
- **司会進行**: ディレクトリ作成、トピック設定、フェーズ/ラウンド管理、参加者通知、合意追跡
- **討論参加者**: 論理的分析、構造化思考（必ず最後に発言）
- **管理ファイル**: `discussions/{topic_id}/queue/topic.yaml`, `consensus.yaml`
- **指示書**: instructions/claude.md

### Gemini（Pane 2）
- **特徴**: 創造的発想、多角的視点
- **通知先**: Claude（discussion:0.0）に発言完了通知
- **指示書**: instructions/gemini.md

### Codex（Pane 1）
- **特徴**: 技術的実装、実用性重視
- **通知先**: Claude（discussion:0.0）に発言完了通知
- **指示書**: instructions/codex.md

### Visualizer（Pane 3）— リッチ可視化
- **CLI**: Gemini CLI (`gemini -y`)
- **特徴**: Mermaid図、画像生成、インフォグラフィック
- **出力ファイル**: `discussions/{topic_id}/visualizer/dashboard.md`, `images/`
- **指示書**: instructions/visualizer.md
- **注意**: 進行管理は行わない（Claudeの責務）

## 討論フェーズ

1. **Opening** (1ラウンド): 初期見解
2. **Debate** (5ラウンド): 意見交換
3. **Synthesis** (2ラウンド): 合意形成

## 禁止事項（全エージェント共通）

1. **ポーリング禁止**: 定期的なファイル確認の繰り返しは禁止
2. **他者ファイル編集禁止**: 自分の発言ファイル以外を編集しない
3. **指揮系統の遵守**: Claude の司会進行に従う

## send-keys の使い方

```bash
# 正しい方法（2段階）
tmux send-keys -t discussion:0.X "メッセージ"
sleep 0.5
tmux send-keys -t discussion:0.X Enter
```

### 通知方向

| From | To | Target | 用途 |
|------|----|--------|------|
| Claude | Gemini | `discussion:0.2` | 方向性指示（パス明示） |
| Claude | Codex | `discussion:0.1` | 方向性指示（パス明示） |
| Claude | Visualizer | `discussion:0.3` | 可視化依頼（パス明示） |
| Gemini | Claude | `discussion:0.0` | 発言完了通知 |
| Codex | Claude | `discussion:0.0` | 発言完了通知 |

## 言語設定

config/settings.yaml の `language` で言語を設定：
- `ja`: 日本語
- `en`: 英語

## 討論開始方法

Claudeペイン（Pane 0, 左上）で以下のように指示：
```
「〇〇について討論を開始してください」
```

Claude（司会進行）が：
1. `discussions/{topic_id}/` ディレクトリを作成
2. `queue/topic.yaml` を設定
3. Gemini/Codex に方向性指示を送信（パス明示）
4. 発言完了通知を待機
5. 自分の分析を記入（最後）
6. `consensus.yaml` を更新
7. Visualizer に可視化依頼を送信（パス明示）
