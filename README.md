# 🎭 Multi-Model Discussion System

複数のAIモデル（Claude, Gemini, Codex）がブレインストーミングや討論を行うtmuxベースのUI。
Claude が司会進行を兼務し、Visualizer は Gemini CLI によるリッチ可視化に特化。
各討論のデータは `discussions/{topic_id}/` に独立して保存される。

## 概要

```
┌──────────────────────────┬──────────────────────────────────┐
│   Pane 0: Claude         │   Pane 2: Gemini                 │
│   🟣 司会進行+論理分析      │   🟢 創造的発想                    │
│   (Claude Code)          │   (Gemini CLI)                   │
├──────────────────────────┼──────────────────────────────────┤
│   Pane 1: Codex          │   Pane 3: Visualizer             │
│   🟡 技術的実装            │   🎨 リッチ可視化                  │
│   (Codex CLI)            │   (Gemini CLI)                   │
└──────────────────────────┴──────────────────────────────────┘
```

## 前提条件

### CLI のインストール

| CLI | インストール |
|-----|-------------|
| Claude Code | `npm install -g @anthropic-ai/claude-code` |
| Gemini CLI | `npm install -g @anthropic-ai/claude-code && npm install -g @anthropic-ai/gemini-cli` もしくは `npm install -g @anthropic-ai/gemini-cli` |
| Codex CLI | `npm install -g @openai/codex` |
| tmux | `brew install tmux` (macOS) |

### 認証の確認

各 CLI は初回起動時にブラウザで OAuth 認証を行う。**討論開始前に全 CLI が認証済みであることを確認すること。**

```bash
# Claude Code — 認証状態の確認
claude doctor
# 未認証の場合: claude login でブラウザが開く

# Gemini CLI — 認証確認（起動して応答すれば OK）
gemini
# 未認証の場合: 初回起動時にブラウザで Google アカウント認証

# Codex CLI — 認証確認
codex login status
# 未認証の場合: codex login でブラウザが開く（ChatGPT アカウント）
```

> **Note**: API キーによる認証も可能（`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, `OPENAI_API_KEY`）。
> 詳細は各 CLI のドキュメントを参照。

## クイックスタート

```bash
# 1. 討論システムを起動
cd discussion
./discussion_start.sh

# 2. セッションにアタッチ
tmux attach -t discussion

# 3. Claudeペイン（左上）で討論を開始
# 「AIの倫理について討論を開始してください」
```

## ディレクトリ構成

```
discussion/
├── discussion_start.sh         # 起動スクリプト
├── config/
│   └── settings.yaml           # 設定ファイル
├── instructions/
│   ├── claude.md               # Claude用指示書（司会進行+論理分析）
│   ├── gemini.md               # Gemini用指示書
│   ├── codex.md                # Codex用指示書
│   └── visualizer.md           # Visualizer用指示書（リッチ可視化）
└── discussions/                # 討論データ（トピックごとに独立）
    ├── ai-ethics/              # 例: AIの倫理
    │   ├── queue/
    │   │   ├── topic.yaml
    │   │   ├── consensus.yaml
    │   │   └── turns/
    │   └── visualizer/
    │       ├── dashboard.md
    │       └── images/
    └── mvp-strategy/           # 例: MVP戦略
        ├── queue/
        └── visualizer/
```

## 各モデルの特徴

| モデル | CLI | 特徴 | 役割 |
|--------|-----|------|------|
| **Claude** | Claude Code | 論理的・構造的 | 司会進行 + 問題を分解し体系的に議論 |
| **Gemini** | Gemini CLI | 創造的・多角的 | 新しい視点、代替案を提示 |
| **Codex** | Codex CLI | 実装志向・実用的 | 技術的実現可能性を評価 |
| **Visualizer** | Gemini CLI | 可視化特化 | Mermaid図・画像生成・インフォグラフィック |

## 討論フロー

```
1. User → Claude(司会): トピック指示

2. Claude: discussions/{topic_id}/ ディレクトリ作成

3. Opening Phase (1ラウンド)
   ├── Claude → Gemini/Codex: 方向性指示（send-keys, パス明示）
   ├── Gemini/Codex → 発言を書く
   ├── Gemini/Codex → Claude: 発言完了通知（send-keys）
   ├── Claude → 自分の分析を書く（最後に発言）
   ├── Claude → consensus.yaml 更新
   └── Claude → Visualizer: 可視化依頼（send-keys, パス明示）

4. Debate Phase (5ラウンド)
   └── 同上のサイクルを繰り返す

5. Synthesis Phase (2ラウンド)
   └── 合意形成、結論導出
```

## 通信プロトコル

### イベント駆動
- ポーリング禁止（API節約）
- YAMLファイルでメッセージ交換
- tmux send-keys で通知（パスを明示）

### 通知方向

| From | To | Target | 用途 |
|------|----|--------|------|
| Claude | Gemini | `discussion:0.2` | 方向性指示（パス明示） |
| Claude | Codex | `discussion:0.1` | 方向性指示（パス明示） |
| Claude | Visualizer | `discussion:0.3` | 可視化依頼（パス明示） |
| Gemini | Claude | `discussion:0.0` | 発言完了通知 |
| Codex | Claude | `discussion:0.0` | 発言完了通知 |

## tmux操作

```bash
# セッションにアタッチ
tmux attach -t discussion

# ペイン間移動
Ctrl+b → 矢印キー

# ペインをズーム（フルスクリーン）
Ctrl+b z

# セッション終了
tmux kill-session -t discussion
```

## 過去の討論を確認する

```bash
# 討論一覧を表示
ls discussions/

# 特定の討論のダッシュボードを確認
cat discussions/ai-ethics/visualizer/dashboard.md

# 特定の討論の発言を確認
ls discussions/ai-ethics/queue/turns/
```

## カスタマイズ

### モデルの追加・変更

`config/settings.yaml` を編集：

```yaml
models:
  - id: claude
    name: "Claude"
    pane: 0
    color: "1;35"
    role: "司会進行 + 論理的分析と構造化思考"
```

### 討論ルールの変更

```yaml
discussion:
  max_rounds: 10        # 最大ラウンド数
  turn_timeout: 120     # ターンタイムアウト（秒）
  consensus_threshold: 2 # 合意に必要な最小モデル数
```

## トラブルシューティング

### セッションが起動しない
```bash
# 既存セッションを強制終了
tmux kill-server
# 再起動
./discussion_start.sh
```

### ペインが応答しない
```bash
# 特定ペインにEnterを送信
tmux send-keys -t discussion:0.X Enter
```

## ライセンス

MIT License
